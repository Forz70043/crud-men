<div class="container mb-2">
    <div class="card">
        <div class="card-header">
            <%= __('Add new Type') %>
        </div>
        <div class="card-body">
            <form action="/types" method="POST" class="was-validate pt-3">
                <div class="form-row">
                    <div class="form-group col"></div>
                    <div class="form-group col-10">
                        <label for="name" class="col-form-label"><%= __('Name') %></label>
                        <input type="text" placeholder="Food" name="name" class="form-control" required/>
                    </div>
                    <div class="form-group col"></div>
                </div>
                <button class="btn btn-primary mb-2" type="submit"><%= __('Submit') %></button>
            </form>
        </div>
    </div>
</div></br>

<div class="container mb-5">
    <div class="card">
        <div class="card-header">
            <%= links %>
        </div>
        <div class="card-body">
            <% if(types && types.length>0) { %>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col"><%= __('Type') %></th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    <% types.forEach(function(type) { %> 
                        <tr>
                            <td><a class="primary font-weight-bold" href="/types/<%= type.id %>"><%= type.name %></a></td>
                            <td>
                                <button class="btn btn-sm btn-warning" type="button" onclick="">
                                    <span class="fa fa-edit"></span>
                                </button>
                                <% type.action='/types'; %>
                                <button class="btn btn-sm btn-danger" type="button" onclick="pushInfoElement(<%= JSON.stringify(type)%>);" data-toggle="modal" data-target="#exampleModalCenter">
                                    <span class="fas fa-trash-alt"></span>
                                </button>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
            <% }else{ %>
                <%- include('nodata') %> 
            <% } %>
        </div>
    </div>
</div>

<%- include('modal') %> 